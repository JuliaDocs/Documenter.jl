language: julia

os:
  - linux
  - osx

julia:
  - 0.7
  - 1.0
  - nightly

notifications:
  email: false

after_success:
  - julia -e 'import Documenter; cd(joinpath(pathof(Documenter), "..", "..", "tests")); include("coverage.jl")'
